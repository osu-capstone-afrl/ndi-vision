#include <sstream>
#include "ros/ros.h"
#include "std_msgs/String.h"

int main() {
	
	// std_msgs::String msg;
	// std::stringstream ss;
	// ss << "It's alive" << count;
	// msg.data = ss.str();

	return 0;
}
